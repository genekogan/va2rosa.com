// ---------------------------------------------------------------------------
// Where the site officially lives. Canonical links, the sitemap, robots.txt
// and the machine-readable files all read from here, so moving house is one
// line rather than a search across the repo.
// ---------------------------------------------------------------------------

export const SITE_URL = 'https://vanessarosa.art';
